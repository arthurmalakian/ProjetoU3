import 'package:app/utils/routes.dart';
import 'package:flutter/material.dart';

class RateUs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("5 Estrelas")),
    );
  }
}
